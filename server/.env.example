# App
PORT=4000

# MongoDB Atlas connection string
# IMPORTANT: Replace placeholders and URL-encode your PASSWORD if it has special characters.
# Example final string:
MONGODB_URI=YOUR_MONGODB_URI_HERE

# Auth
# Generate a strong secret (example):
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=un-secreto-fuerte
JWT_EXPIRES_IN=1d

# Frontend URL for links
FRONTEND_URL=http://localhost:3000

# SendGrid (email delivery)
# You must verify the sender in SendGrid (Single Sender or authenticated domain)
SENDGRID_API_KEY=YOUR_SENDGRID_KEY_HERE
SENDGRID_FROM="INHALEX <no-reply@inhalex.local>"
